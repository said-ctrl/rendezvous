{% extends 'base.html.twig' %}

{% block title %}Modifier Réunion{% endblock %}

{% block body %}
    <h1>Modifier la réunion</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Enregistrer les modifications</button>
    {{ form_end(form) }}
    <a href="{{ path('meeting_index') }}">Retour à la liste</a>
{% endblock %}
